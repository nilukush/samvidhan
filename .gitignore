# Agent and session files (never commit)
docs/
CLAUDE.md
AGENTS.md
MEMORY.md

# Environment
.env
.env.*
!.env.example

# Generated content collections (created by npm run data:sync)
src/content/articles/
src/content/parts/
src/content/schedules/

public/vendor/

public/models/

public/vectors/

# Dependencies and build output
node_modules/
dist/
.astro/
coverage/
playwright-report/
test-results/

# Vendored source PDFs are committed by design (data/source/) but extraction
# scratch output is not
data/scratch/

# OS noise
.DS_Store

# Transient detector report (the workflow attaches it to issues instead)
data/ritual/last-report.md

# Hindi raw extraction cache (OCR + layer dumps, regenerated locally; see scripts/extract/hindi/raw.ts)
data/raw-hindi/
