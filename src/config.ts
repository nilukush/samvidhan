/**
 * Site wide identity tokens. Single source of truth for the site name,
 * tagline, and canonical URL, consumed by pages, tests, and the build.
 */
export const SITE_NAME = 'Samvidhan';

export const SITE_TAGLINE = 'The Constitution of India, made readable.';

/** Canonical production URL. Free Cloudflare Pages subdomain per project decision. */
export const SITE_URL = 'https://samvidhan.pages.dev';
